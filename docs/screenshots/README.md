# Screenshot checklist

Replace the four bracketed placeholders in the main README with these real images:

1. `hero-clock-town.png` — Mario standing clearly in Clock Town beside Tatl, with the
   normal Majora's Mask HUD visible. Use this as the first, widest image.
2. `brothers-mask.png` — the Brother's Mask selected in the pause-menu inventory,
   with its description readable.
3. `mario-movement.png` — Mario performing a recognisable move in Termina, ideally a
   triple jump or ground-pound with a clear silhouette.
4. `builder.png` — the builder window with both inputs filled and the build button
   visible. Hide personal folder names and do not show real ROM filenames if they
   contain personal information.

Use the release build with no debug text, emulator menus, desktop clutter, or
diagnostic overlays. Prefer PNG, crop gameplay shots to the game viewport, and keep
each file below 2 MiB.
